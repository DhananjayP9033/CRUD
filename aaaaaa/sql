drop table Employee;

select * from Employee;

create table Employee 
(id int GENERATED ALWAYS AS IDENTITY(Start with 100, Increment by 1), 
name varchar(50), email varchar(50),moNumber varchar(20));
